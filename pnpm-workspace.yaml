packages:
  - examples/*
  - scripts/*

onlyBuiltDependencies:
  - esbuild
  - puppeteer

catalog:
  typescript: ~6.0.0

includeWorkspaceRoot: true

allowBuilds:
  esbuild: true
  puppeteer: true

publicHoistPattern:
  - '@oxc-project/types' # we need rolldown's exact version but they don't re-export
